# Calibration samples

Real deliverables (with permission to quote) go here as `<n>-<slug>/` folders containing `scope.md`, the delivered files, and `expected.json` (`{"expected":"PASS|NEEDS_REVIEW|FAIL","note":"…"}`). Target before submission: 20 real + the 10 adversarial samples. `pnpm calibrate` prints the confusion matrix that is copied into the root README. No sample, no number.
