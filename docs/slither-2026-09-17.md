# Slither report — Vault v4, 2026-09-17

Command (Slither 0.11 via `uv tool install slither-analyzer`, Foundry build):

```
cd contracts && slither . --filter-paths "lib/|test/|script/" --exclude-dependencies
```

31 contracts, 102 detectors, 33 results. Every result was read against the code; none required a change before the testnet deploy. Two hardening items were queued for the mainnet release (v5) because any contract edit means a redeploy on both testnets; both landed on 2026-09-22 (see the v5 notes below) and ship with the mainnet deploy.

| Impact | Detector | Where | Verdict |
|---|---|---|---|
| High | reentrancy-balance | `depositWithAuthorization`: balance read before `receiveWithAuthorization`, delta checked after | **False positive.** The balance delta is the fee-on-transfer-safe way to learn what arrived; the function is `nonReentrant`, and the token is owner-allow-listed (USDC / pathUSD). A reentrant call cannot change the delta's meaning. |
| Medium | incorrect-equality (2) | `received == 0` in `deposit` and `depositWithAuthorization` | **False positive.** A zero-check on a computed delta, not a balance equality. |
| Medium | reentrancy-no-eth (6) | `settle`, `autoSettle`, `resolve`, `refundExpired`, `_payout`, `_deployToEarn`: Earn vault calls (`withdrawExact` / `redeem` / `deposit`) precede `job.status` and `balances` writes | **Accepted, guarded.** Every entrypoint that reaches an Earn vault is `nonReentrant` (`fund`, `settle`, `settleWithSig`, `autoSettle`, `resolve`, `refundExpired`, the Earn and Zone functions). The unguarded functions (`submit`, `dispute`, `resubmit`, `attest`, `attributeDeposit`, `createJob`) make no external calls and are party- or role-gated, so a venue cannot reach them mid-call. Earn vaults are owner-allow-listed. **v5 (done 2026-09-22):** `_settle`, `autoSettle`, `resolve` and `refundExpired` write the terminal `job.status` and release `locked` before `_payout` / `_recall` reach the venue, so the order is checks-effects-interactions even without the guard. `test/VaultReentrancy.t.sol` has a venue that inspects the Vault mid-call on every path (status already terminal, lock already released, earn shares already cleared) and tries to re-enter (stopped by the guard). |
| Medium | unused-return (2) | `_withdrawToZone` ignores `depositEncrypted`'s `newCurrentDepositQueueHash` | **Accepted.** The queue hash is the portal's bookkeeping; the Vault records the transfer in `WithdrawnToZone`. |
| Low | missing-zero-check (4) | `arbiter` / `intake` in the constructor and setters | **v5 (done 2026-09-22):** the constructor, `setArbiter` and `setIntake` revert with `ZeroAddress`. Owner-only; a zero arbiter would only have blocked `resolve` until reset. |
| Low | reentrancy-benign (8) | event emission after Earn / portal calls | Same guard argument as above; events only. |
| Low | timestamp (4) | deadlines (`submitDeadline`, `reviewWindow`, signature `deadline`) | **Accepted.** Windows are hours to days; validator drift of seconds is immaterial. |
| Info | assembly, naming-convention, unindexed-event-address (4) | `_useSig` selector check; event params | Style. `EarnRedeemed` / `EarnDeposited` keep the vault address unindexed on purpose (three indexed slots are used). |

CI runs the same job as informational (`slither (informational)` in `.github/workflows/ci.yml`) and uploads the SARIF.
